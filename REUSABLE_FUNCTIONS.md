# Reusable Functions

This legacy reference file is kept for backward compatibility.

- Canonical function, hook, util, service, and validator docs now live in `functions.md`.
- Canonical reusable component docs now live in `components.md`.
